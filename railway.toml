[build]
builder = "nixpacks"

[deploy]
# Pure Node static server — binds 0.0.0.0:$PORT (Railway injects PORT)
startCommand = "node server.js"
healthcheckPath = "/health"
healthcheckTimeout = 30
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 5
