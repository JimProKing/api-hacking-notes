[build]
builder = "nixpacks"

[deploy]
# Static site: serve listens on Railway-injected $PORT
startCommand = "npx --yes serve -s . -l tcp://0.0.0.0:$PORT"
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 3
